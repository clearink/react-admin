import React from "react"
import { IBaseProps } from "@/@types/fc"

function CardList(props: IBaseProps) {
	return (
		<div className='flex justify-center items-center w-full min-h-full'>
			CardList
		</div>
	)
}

export default CardList
