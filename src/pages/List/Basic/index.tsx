import React from "react"
import { IBaseProps } from "@/@types/fc"

function BasicList(props: IBaseProps) {
	return (
		<div className='flex justify-center items-center w-full min-h-full'>
			BasicList
		</div>
	)
}

export default BasicList
